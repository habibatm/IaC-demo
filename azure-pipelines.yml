trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: |
    python -m pip install --upgrade pip
    pip install checkov
    checkov -d vulnerable --framework terraform
  displayName: IaC security scan with Checkov
